name: Setup project
on:
  push:
    branchs: [ stage, main, master ]

jobs:
  Install dependencies:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Root composer dependencies
        run: |
          composer install -o
